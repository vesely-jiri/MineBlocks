plugins {
    // Lets Gradle download the Java 25 toolchain automatically when it is not installed.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'MineBlocks'
